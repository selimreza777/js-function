// Task-1
// Take four parameters. Multiply the four numbers and then return the result
// write a code 
function multiplyNumbers(num1, num2, num3, num4) {
  const multi = num1 * num2 * num3 * num4;
  return multi;
}
const multis = multiplyNumbers(15, 2, 3, 2);
console.log('Multiply four parameters is:', multis);